package com.example.app.entity.enums;

/** Type of domain entity a notification links to (used to build deep-link URLs on the UI). */
public enum NotificationRelatedEntity {
    BOOKING,
    TICKET,
    COMMENT,
    SYSTEM,
    NONE
}
